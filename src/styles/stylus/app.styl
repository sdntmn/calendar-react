.app
  margin: 0 auto;
  padding: 0;
  background-color: #FFFFFF;
  min-height: 100vh;
  display: grid;
  grid-template-rows: 100px 1fr;
  width: 100%

  &__rotation
    transform: rotate(-180deg);

.page__section {
  margin: 0;
  padding: 0;
}
